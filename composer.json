{
    "name": "kilbiller/kulid",
    "description": "Generate cool ids a la Stripe",
	"type": "library",
	"license": "MIT",
    "authors": [
        {
            "name": "Rémy Peru",
            "email": "peru.remy@gmail.com"
        }
    ],
    "require": {
		"php": ">=7.0"
	},
	"require-dev": {
		"kahlan/kahlan": "^4.0.1"
	},
	"autoload": {
		"psr-4": {
			"Kulid\\": "src/"
		}
	},
	"scripts": {
		"test": "kahlan --spec=tests --grep=*Test.php",
		"test:coverage": "kahlan --spec=tests --grep=*Test.php --coverage=4 --clover=clover.xml"
	}
}
